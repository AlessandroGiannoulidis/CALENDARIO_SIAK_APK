<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/event_card_background" />
    <corners android:radius="@dimen/event_card_corner_radius" />
    <!-- Subtle border for card definition -->
    <stroke
        android:width="0.5dp"
        android:color="@color/overlay_light" />
</shape>